group 'it.emarolab.sit'
version = project.catkin.pkg.version

repositories {
    maven {
        url "../../../../devel/share/maven/"
    }
}

dependencies {
    compile 'org.ros.rosjava_messages:injected_armor_msgs:2.0.0'
    compile project(':owloop')
    
}
